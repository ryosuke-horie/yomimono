# BFF 経由で外部APIへアクセスするためのサーバー専用設定
BFF_API_BASE_URL=http://localhost:8787
# APIキーなどクライアントに露出させない値は必ずサーバー専用の環境変数を使う
BFF_API_KEY=

# 既存のクライアント直呼びが残っている間の後方互換
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787
